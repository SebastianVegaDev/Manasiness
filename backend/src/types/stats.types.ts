export type StatsSummaryRow = {
    day_total: string | number
    day_count: string | number
    week_total: string | number
    week_count: string | number
    month_total: string | number
    month_count: string | number
}

export type StatsSummary = {
    dayTotal: number
    dayCount: number
    weekTotal: number
    weekCount: number
    monthTotal: number
    monthCount: number
}