import type { UserOptionRow, UserRow } from "../../types/person.types.js"

export function mapWorkers(rows: UserRow[]): UserRow[] {
    return rows
}

export function mapWorkerOptions(rows: UserOptionRow[]): UserOptionRow[] {
    return rows
}