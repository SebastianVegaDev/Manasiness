import type { UserOptionRow, UserRow } from "../../types/person.types.js"

export function mapSuppliers(rows: UserRow[]): UserRow[] {
    return rows
}

export function mapSupplierOptions(rows: UserOptionRow[]): UserOptionRow[] {
    return rows
}