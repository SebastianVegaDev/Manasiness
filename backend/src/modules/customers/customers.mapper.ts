import type { UserOptionRow, UserRow } from "../../types/person.types.js"

export function mapCustomers(rows: UserRow[]): UserRow[] {
    return rows
}

export function mapCustomerOptions(rows: UserOptionRow[]): UserOptionRow[] {
    return rows
}